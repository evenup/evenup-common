fixtures:
  repositories:
  forge_modules:
    beaver:
      repo: "evenup/beaver"
      ref: "1.3.0"
    concat:
      repo: "puppetlabs/concat"
      ref: "1.2.0"
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "4.2.0"
  symlinks:
    common: "#{source_dir}"
